#:kivy 1.0.9

<Ball>:
    size: 50, 50 
    canvas:
        Ellipse:
            pos: self.pos
            size: self.size

<Paddle>:
    size: 200, 25
    canvas:
        Rectangle:
            pos: self.pos
            size: self.size

<Game>:
    canvas.before:
        Rectangle:
            size: self.size
            pos: self.pos
            source: "sky.png"
        Rectangle:
            size: self.width, 138
            pos: self.pos[0], self.pos[1] + self.height - 138
            texture: self.cloud_texture
        Rectangle:
            size: self.width, 254
            pos: self.pos[0] + 20, self.pos[1] + self.height - 300
            texture: self.sun_texture

    ball: bb_ball
    player1: bb_player
    
    Ball:
        id: bb_ball
        center: self.parent.center
        
    Paddle:
        id: bb_player
        y: root.y + 100
        center_x: root.center_x

    Label:
        font_size: 70  
        center_x: root.center_x
        top: root.top - 100
        text: str(root.player1.score)

    # SandPaddle:
    #     id: sandpaddle
    #     source: "sandpaddle.png"

    # GreenPaddle:
    #     id: greenpaddle
    #     source: "greenpaddle.png"

    # BluePaddle:
    #     id: bluepaddle
    #     source: "bluepaddle.png"
 